title.projectUrl=Axivion Server URL
description.projectUrl=This is the URL to the project on the Axivion Server.
title.basedir=Base Directory
description.basedir=This is the base directory in your workspace (must match the projectdir setting of analysis). You can use jenkins environment variables here, e.g. $WORKSPACE
title.credentialsId=Credentials ID
description.credentialsId=The ID of the credentials (as stored in jenkin's credential store) used for authentication against the Axivion Server.
