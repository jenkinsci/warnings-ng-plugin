<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

  <f:dropdownDescriptorSelector field="property" title="${%Property}" />

  <f:entry title="${%Include filter regular expression}" description="${%Name TODO}" field="regexp">
    <f:textbox/>
  </f:entry>

  <f:entry title="">
    <div align="right">
      <f:repeatableDeleteButton />
    </div>
  </f:entry>

</j:jelly>
