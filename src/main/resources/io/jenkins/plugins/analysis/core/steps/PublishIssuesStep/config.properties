title.sourceCodeEncoding=Source Code Encoding
description.sourceCodeEncoding=Encoding of your source code. \
    Leave this field empty to use the default encoding of the platform (not recommended).

title.name=Custom Name
description.name=Optional custom display name of the tool, overwrites the default name of the registered tool.
title.id=Custom ID
description.id=Optional custom ID (URL) of this tool, overwrites the default ID of the registered tool.