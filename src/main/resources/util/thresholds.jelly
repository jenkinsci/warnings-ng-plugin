<!--
  Section header

  <%@attribute name="id" required="true" %>
-->

<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler"
  xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson"
  xmlns:f="/lib/form" xmlns:u="/util">

  <f:entry title="${%Health thresholds}" description="${%description.health.limit}">
    <table>
      <tbody>
        <tr>
          <td>
            <img src="${rootURL}/images/16x16/health-80plus.gif" alt="100%" title="${%description.healthy}" /> ${%threshold.100.percent}
          </td>
          <td>
            <f:textbox name="healthy" value="${instance.healthy}" />
          </td>
          <td width="20"/>
          <td>
            <img src="${rootURL}/images/16x16/health-00to19.gif" alt="0%" title="${%description.unhealthy}" /> ${%threshold.0.percent}
          </td>
          <td>
            <f:textbox name="unHealthy" value="${instance.unHealthy}" />
          </td>
        </tr>
      </tbody>
    </table>
  </f:entry>
  <f:entry title="${%Health priorities}" description="${%description.health.priorities}">
    <f:radio name="${id}.thresholdLimit" value="high" checked="${instance.thresholdLimit == 'high'}" id="thresholdLimit.high"/>
    <st:nbsp/>
    <label for="thresholdLimit.high">${%Only priority high}</label>
    <st:nbsp />
    <f:radio name="${id}.thresholdLimit" value="normal" checked="${instance.thresholdLimit == 'normal'}" id="thresholdLimit.normal"/>
    <st:nbsp/>
    <label for="thresholdLimit.normal">${%Priorities high and normal}</label>
    <st:nbsp />
    <j:if test="${instance != null}">
      <f:radio name="${id}.thresholdLimit" value="low" title="" checked="${instance.thresholdLimit == 'low'}" id="thresholdLimit.low"/>
    </j:if>
    <j:if test="${instance == null}">
      <f:radio name="${id}.thresholdLimit" value="low" title="" checked="${true}" id="thresholdLimit.low"/>
    </j:if>
    <st:nbsp/>
    <label for="thresholdLimit.low">${%All priorities}</label>
    <st:nbsp />
  </f:entry>
  <f:entry title="${%Status thresholds}" description="${%description.status}">
    <table>
      <thead>
        <tr>
          <td />
          <td>${%All priorities}</td>
          <td>${%Priority high}</td>
          <td>${%Priority normal}</td>
          <td>${%Priority low}</td>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>
            <img src="${rootURL}/images/16x16/yellow.gif" alt="100%" title="${%description.unstableTotal.all}" /> ${%Total}
          </td>
          <td>
            <f:textbox name="unstableTotalAll" value="${instance.unstableTotalAll}" />
          </td>
          <td>
            <f:textbox name="unstableTotalHigh" value="${instance.unstableTotalHigh}" />
          </td>
          <td>
            <f:textbox name="unstableTotalNormal" value="${instance.unstableTotalNormal}" />
          </td>
          <td>
            <f:textbox name="unstableTotalLow" value="${instance.unstableTotalLow}" />
          </td>
        </tr>
        <tr>
          <td>
            <img src="${rootURL}/images/16x16/yellow.gif" alt="100%" title="${%description.unstableNew.all}" /> ${%New}
          </td>
          <td>
            <f:textbox name="unstableNewAll" value="${instance.unstableNewAll}" />
          </td>
          <td>
            <f:textbox name="unstableNewHigh" value="${instance.unstableNewHigh}" />
          </td>
          <td>
            <f:textbox name="unstableNewNormal" value="${instance.unstableNewNormal}" />
          </td>
          <td>
            <f:textbox name="unstableNewLow" value="${instance.unstableNewLow}" />
          </td>
        </tr>
        <tr>
          <td>
            <img src="${rootURL}/images/16x16/red.gif" alt="100%" title="${%description.failedTotal.all}" /> ${%Total}
          </td>
          <td>
            <f:textbox name="failedTotalAll" value="${instance.failedTotalAll}" />
          </td>
          <td>
            <f:textbox name="failedTotalHigh" value="${instance.failedTotalHigh}" />
          </td>
          <td>
            <f:textbox name="failedTotalNormal" value="${instance.failedTotalNormal}" />
          </td>
          <td>
            <f:textbox name="failedTotalLow" value="${instance.failedTotalLow}" />
          </td>
        </tr>
        <tr>
          <td>
            <img src="${rootURL}/images/16x16/red.gif" alt="100%" title="${%description.failedNew.all}" /> ${%New}
          </td>
          <td>
            <f:textbox name="failedNewAll" value="${instance.failedNewAll}" />
          </td>
          <td>
            <f:textbox name="failedNewHigh" value="${instance.failedNewHigh}" />
          </td>
          <td>
            <f:textbox name="failedNewNormal" value="${instance.failedNewNormal}" />
          </td>
          <td>
            <f:textbox name="failedNewLow" value="${instance.failedNewLow}" />
          </td>
        </tr>
      </tbody>
    </table>
  </f:entry>
  <f:entry title="${%Use delta for new warnings}" description="${%description.useDeltaValues}">
    <f:checkbox name="useDeltaValues" checked="${instance.useDeltaValues}"/>
  </f:entry>

</j:jelly>
