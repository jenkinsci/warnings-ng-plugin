<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

  <f:entry title="${%title.threshold}" description="${%description.threshold}" field="integerThreshold">
    <f:number default="1"/>
  </f:entry>

  <f:entry title="${%title.type}" description="${%description.type}" field="type">
    <f:select/>
  </f:entry>

  <f:entry title="${%title.warning}" field="criticality">
    <f:select/>
  </f:entry>

</j:jelly>
